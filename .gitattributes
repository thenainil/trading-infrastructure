# Third-party CMake/Conan integration glue. Keep it in the repo, but do not let
# it dominate GitHub's language breakdown.
conan_provider.cmake linguist-vendored

# Project headers are C++ headers even though they use the .h extension.
include/**/*.h linguist-language=C++

# Generated local build output if it ever appears in a branch.
conan/** linguist-generated
